// schedule/seed/index.js

import { renderTable } from "./seedList-render.js";

/* ============================================================
   播種計画（seed モード）の描画
============================================================ */
export function renderSeedList() {
  renderTable();
}
